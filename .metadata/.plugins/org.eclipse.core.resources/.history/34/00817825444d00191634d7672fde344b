server:
  port: 9100
spring:
  application:
    name: eureka-server
eureka:
  instance:
    hostname: 127.0.0.1
  client:
    service-url:
      defaultZone: http://
    ##是否注册到eureka
    register-with-eureka: true
    ##是否拉去eureka服务上的信息
    fetch-registry: true        